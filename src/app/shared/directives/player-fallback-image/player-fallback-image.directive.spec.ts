import { PlayerFallbackImageDirective } from './player-fallback-image.directive';

describe('PlayerFallbackImageDirective', () => {
  it('should create an instance', () => {
    const directive = new PlayerFallbackImageDirective();
    expect(directive).toBeTruthy();
  });
});
