import { FallbackSearchDirective } from './fallback-search.directive';

describe('FallbackSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new FallbackSearchDirective();
    expect(directive).toBeTruthy();
  });
});
