import { CcodeToCountryPipe } from './ccode-to-country.pipe';

describe('CcodeToCountryPipe', () => {
  it('create an instance', () => {
    const pipe = new CcodeToCountryPipe();
    expect(pipe).toBeTruthy();
  });
});
