import { PlayerImagePipe } from './player-image.pipe';

describe('PlayerImagePipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerImagePipe();
    expect(pipe).toBeTruthy();
  });
});
