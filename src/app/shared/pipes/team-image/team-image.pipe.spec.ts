import { TeamImagePipe } from './team-image.pipe';

describe('TeamImagePipe', () => {
  it('create an instance', () => {
    const pipe = new TeamImagePipe();
    expect(pipe).toBeTruthy();
  });
});
